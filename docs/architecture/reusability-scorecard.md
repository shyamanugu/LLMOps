# Reusability Scorecard

Filled in during Phase 3 (`docs/checklist/BUILD-CHECKLIST.md`) once usecase #2's onboarding is actually attempted — **not before**. This file is a template until then; do not populate it with estimated or hypothetical numbers.

## Usecase #2: [name]

| Metric | Value |
|---|---|
| Files changed under `platform/services/**` | |
| Config entries added (list which files) | |
| Time to onboard (start to first successful pipeline run) | |
| % platform code reused vs. net-new | |
| Any platform gap found (usecase required a platform code change) | |

## Platform gaps found (if any)
List each one: what broke, why the existing mechanism didn't cover it, and how it was fixed generically — never as a usecase-specific patch. See `docs/architecture/onboarding-runbook.md`'s "What counts as a platform code change" section.
