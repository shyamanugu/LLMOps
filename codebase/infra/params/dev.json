{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "_comment": "Parameters for the DEV environment. Deploy with: az deployment group create -g <rg-dev> -f infra/main.bicep -p @infra/params/dev.json",
  "parameters": {
    "namePrefix": { "value": "llmops" },
    "environmentName": { "value": "dev" },
    "location": { "value": "eastus2" },
    "acrName": { "value": "" },
    "_acrName_TODO": "TODO(env): set your shared ACR name (e.g. acrllmops) so images pull keylessly.",
    "privateNetworking": { "value": false },
    "tags": {
      "value": {
        "costCenter": "TODO",
        "owner": "platform-team"
      }
    }
  }
}
