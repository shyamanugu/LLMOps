# Base dependencies (the skeleton runs offline with just these).
python-dotenv>=1.0

# --- Optional: uncomment to use real Azure OpenAI instead of mock mode ---
# openai>=1.40
# azure-identity>=1.17

# --- Optional: real RAG over Azure AI Search ---
# azure-search-documents>=11.5

# --- Optional: guardrails via Azure AI Content Safety ---
# azure-ai-contentsafety>=1.0

# --- Optional: richer evaluation metrics ---
# ragas>=0.2
# deepeval>=1.0

# --- Optional: LLM observability (self-hosted) ---
# langfuse>=2.50
