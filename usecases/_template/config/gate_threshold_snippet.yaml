# Paste this block under `usecases:` in
# platform/services/04-evaluation-gate/config/gates.yaml, replacing
# REPLACE_WITH_USECASE_NAME with this usecase's actual name. Omit this
# entirely to use the platform default (100% — every golden-dataset case
# must pass).
REPLACE_WITH_USECASE_NAME:
  dev:
    pass_threshold: 1.0
