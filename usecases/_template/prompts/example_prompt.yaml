name: example_prompt
version: 1
description: >
  Placeholder prompt — replace with this usecase's real prompts. Schema
  matches platform/services/02-prompt-management's PromptSpec; see that
  component's README.
model_capability: nano
input_variables: [message]
template: |
  {{fragment:safety_preamble}}

  Respond to: {message}
