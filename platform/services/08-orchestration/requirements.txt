pytest>=8.0

# This component imports model_management and prompt_management directly via
# pythonpath (see ADR 0004 and pytest.ini) rather than as packaged
# dependencies — their requirements need to be installed in the same
# environment too.
-r ../03-model-management/requirements.txt
-r ../02-prompt-management/requirements.txt
